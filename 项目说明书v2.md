# 水稻叶片病害检查系统 V2 — 项目说明书（本地版）

## 1. 项目背景与定位

### 1.1 V1 vs V2 的区别

| 维度 | V1（rice-leaf-android/server） | V2（rice-leaf-local） |
|------|-------------------------------|----------------------|
| 架构 | Android + Java 后端 + Python ML 微服务 | Android 纯本地 |
| 网络 | 需要连接后端服务器 | **完全离线，无需网络** |
| 推理 | 后端调用 Python Flask 服务 | **手机端 ONNX / TorchScript 本地推理** |
| 数据存储 | MySQL/H2 数据库 + 文件服务器 | **Room 本地数据库 + 手机文件系统** |
| 模型部署 | 服务器端 GPU/CPU | **打包进 APK，assets 目录** |
| 适用场景 | 联网田间调查 | 无网络/弱网野外环境 |

### 1.2 设计目标

将水稻叶片病害检测能力完全本地化部署到 Android 设备，实现：
- 拍摄 → 本地模型推理 → 结果展示，全流程在手机端完成
- 模型可按需切换（默认模型 / 用户导入模型）
- 数据（图片 + 记录）完全保留在手机本地，支持导出

---

## 2. 技术选型

| 层次 | V1 技术 | V2 技术 |
|------|--------|--------|
| UI | Jetpack Compose + Material3 | Jetpack Compose + Material3（复用） |
| 相机 | CameraX | CameraX（复用） |
| 推理引擎 | Python PyTorch（服务端） | **ONNX Runtime Mobile** 或 **PyTorch Mobile** |
| 本地数据库 | Room（用于缓存） | Room（主存储） |
| 图片存储 | 服务器磁盘 | 手机内部存储 / 外部存储 |
| 模型格式 | .pth（PyTorch） | **.onnx** 或 **.ptl**（TorchScript） |
| 模型管理 | 无 | SharedPreferences + 文件复制 |
| 导出 | 后端 Apache POI | **Apache POI（Android 端直接生成）** |
| 依赖注入 | Hilt | Hilt（复用） |

---

## 3. 模型管理设计

### 3.1 默认模型

- 由开发者使用 `train.py` 训练，导出为 ONNX 或 TorchScript 格式
- 放置在 `app/src/main/assets/models/default_rice_leaf.onnx`
- APK 安装时随 assets 一起打包到手机
- 首次启动 App 时自动从 assets 复制到 `{app_files}/models/` 目录

### 3.2 模型文件规范

每个模型需要配套一个 `model_info.json` 描述文件：

```json
{
  "name": "水稻病害检测 v1.0",
  "version": "1.0.0",
  "format": "onnx",
  "input_size": [224, 224],
  "normalize": { "mean": [0.485, 0.456, 0.406], "std": [0.229, 0.224, 0.225] },
  "classes": ["健康", "叶烫病", "白叶枯病", "稻瘟病", "窄褐斑病", "纹枯病", "胡麻叶斑病"],
  "severity_thresholds": [[0,0],[1,5.0],[3,10.0],[5,25.0],[7,50.0],[9,100.0]],
  "symptom_desc": {
    "健康": "叶片健康，无明显病斑",
    "叶烫病": "叶尖或叶缘呈水渍状褪绿，后变黄白色干枯",
    "白叶枯病": "沿叶缘或叶脉呈黄白色长条状，病健界限呈波浪纹",
    "稻瘟病": "梭形或圆形斑，边缘褐色，中央灰白色",
    "窄褐斑病": "叶片上出现细窄褐色条斑，宽1-2mm",
    "纹枯病": "叶鞘及叶片上云纹状大斑，边缘深褐色",
    "胡麻叶斑病": "芝麻粒大小褐色至红褐色斑点，边缘黄色晕圈"
  }
}
```

### 3.3 模型选择机制

- **设置 → 模型选择**：显示所有已安装模型列表
- 每个模型显示：名称、版本、格式、类别数
- 当前使用的模型高亮标记
- 切换模型后立即生效，下次拍照使用新模型

### 3.4 用户添加模型

支持两种方式导入：

**方式一：从文件选择**
1. 点击「添加模型」
2. 系统文件选择器打开，过滤 `.onnx` / `.ptl` 文件
3. 选择模型文件后，提示用户填写或上传 `model_info.json`
4. 如未提供 info 文件，引导用户手动填写：类别名称列表、输入尺寸、归一化参数
5. 所有文件复制到 `{app_files}/models/{model_name}/` 目录

**方式二：从 URL 下载**
1. 用户输入模型下载地址
2. App 后台下载模型文件和 info 文件
3. 校验文件完整性（MD5/SHA256，可选）
4. 存放到 models 目录

### 3.5 模型更新

- 设置中提供「检查模型更新」按钮
- 向开发者配置的更新地址查询最新模型版本号
- 如有新版本，提示下载更新
- 默认模型不覆盖，用户可选择保留旧版本

---

## 4. 拍照界面设计

### 4.1 整体布局

```
叶片位可以添加一个默认选择，在下面可以手动编辑添加更多的叶片位
┌──────────────────────────────────────────────┐
│  ┌──────────────────────────────────────────┐ │
│  │                                          │ │
│  │          CameraX 实时预览                  │ │
│  │          (全屏取景)                        │ │
│  │                                          │ │
│  │    ┌──────────────────────────┐          │ │
│  │    │ ✓ 识别完成                │          │ │  ← 结果显示弹窗
│  │    │ 稻瘟病（叶瘟）置信度 92%   │          │ │     (悬浮在顶部)
│  │    │ 病斑面积: 15.5%  级别: 5  │          │ │
│  │    └──────────────────────────┘          │ │
│  │                                          │ │
│  └──────────────────────────────────────────┘ │
├──────────────────────────────────────────────┤
│   株号: [__]  叶片位: [倒3叶▼]                 │  ← 参数区
├──────────────────────────────────────────────┤
│    [✕ 废片]          [⭕ 拍摄]          [✓ 确认]│  ← 控制区
│   (蓝色/灰色)       (大圆形白色)      (蓝色/灰色)│
└──────────────────────────────────────────────┘
```

### 4.2 交互流程

```
[进入拍照] → 相机预览启动 → 参数区显示(上次值/默认值)
     ↓
[调整参数] → 用户填写株号/叶片位
     ↓
[点击 ⭕ 拍摄] → CameraX 拍照 → Bitmap 暂存到内存
     ↓
[底部按钮切换] → 左边 [✕] 右边 [✓] 中间恢复 [⭕] 可重拍
     ↓
[点击 ✓ 确认] → 调用本地 ONNX 模型推理 → 结果弹窗在顶部浮现（可手动滑走，或点击查看详细信息）
     ↓
[自动保存] → Room 写入记录 + 保存图片到本地
     ↓
[参数递推] → 不需要递推，用户手动选择
     ↓
[继续拍摄] → 循环，相机预览不中断
```

### 4.3 底部三段式按钮状态

| 状态 | 左按钮(✕) | 中按钮(⭕) | 右按钮(✓) |
|------|----------|-----------|----------|
| **待拍摄** | 隐藏/灰色 | 可点击 | 隐藏/灰色 |
| **已拍摄未确认** | 可点击（丢弃） | 可点击（重拍） | 可点击（确认识别） |丢弃后不应该记录数据或者数据更新
| **识别中** | 灰色 | 加载动画 | 灰色 |  我希望点击确认识别后，由本地处理后通过弹窗显示，提高拍摄效率，不要等待返回结果

### 4.4 结果显示弹窗

- 位置：拍摄预览区**顶部**，半透明背景，不遮挡取景
- 显示内容：病害名称、置信度（百分比）、病斑面积占比、病情级别
- 右上角 [✕] 关闭弹窗
- 3 秒后自动淡出（可配置）
- 弹窗出现时相机继续预览，不影响连续拍摄

### 4.5 拍摄参数显示与递推

参数区始终显示在预览区下方：

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| 株号 | 文本输入 | "1" |
| 叶片位 | 下拉选择 | "倒3叶" | 倒3叶 / 倒2叶 / 倒1叶/添加叶片位 |


所有参数均可手动修改，递推值不覆盖手动修改的值。

---

## 5. 本地推理模块

### 5.1 模块结构

```
app/src/main/java/com/riceleaf/local/ml/
├── ModelManager.kt          // 模型加载、切换、列表管理
├── OnnxInferenceEngine.kt   // ONNX Runtime 推理封装
├── ImagePreprocessor.kt     // 图片预处理（resize, normalize, HWC→CHW）
├── LesionEstimator.kt       // 病斑面积计算（HSV 色彩空间）
└── SeverityMapper.kt        // 病斑面积 → 病情级别映射
```

### 5.2 推理流程

```
Bitmap (CameraX 输出)
  ↓
ImagePreprocessor:
  1. 缩放到模型输入尺寸（224×224）
  2. RGB 归一化 [0,1]
  3. 减均值除标准差: (x - mean) / std
  4. HWC → CHW → 添加 batch 维度 [1,3,224,224]
  ↓
OnnxInferenceEngine:
  1. 创建 OrtSession
  2. 输入 tensor → 运行推理
  3. 输出 logits → softmax → top1 / top3
  ↓
LesionEstimator:
  1. BGR → HSV
  2. 绿色掩膜 (H:25-90, S:40-255, V:40-255)
  3. 叶片掩膜 (灰度 > 30)
  4. 病斑 = 叶片像素 - 绿色像素
  5. 面积比 = 病斑 / 叶片 × 100%
  ↓
SeverityMapper:
  ratio 映射到级别: 0(0%), 1(≤5%), 3(≤10%), 5(≤25%), 7(≤50%), 9(>50%)
  ↓
返回: DetectResult(diseaseName, confidence, lesionRatio, severity, symptom)
  ↓
保存: Room insert + 图片写入磁盘
```

### 5.3 模型格式转换（开发者侧）

训练完成后将 `.pth` 转为 ONNX：

```python
import torch
from model import load_model
import config

model = load_model(config.OUTPUT_MODEL_PATH, config.NUM_CLASSES)
model.eval()

dummy = torch.randn(1, 3, 224, 224)
torch.onnx.export(model, dummy, "default_rice_leaf.onnx",
                  input_names=["input"],
                  output_names=["output"],
                  dynamic_axes={"input": {0: "batch"}},
                  opset_version=12)
```

---

## 6. 本地数据存储

### 6.1 Room 数据库

与 V1 数据库结构一致，但完全本地化：

```
┌──────────────┐       ┌──────────────┐
│ disease_dict │       │    record    │
├──────────────┤       ├──────────────┤
│ id (PK)      │ ←──   │ id (PK)      │
│ name         │       │ sample_point │
│ symptom      │       │ plant_no     │
│ spot_feature │       │ leaf_position│
│ sort_order   │       │ disease_id(FK)│
└──────────────┘       │ confidence   │
                       │ lesion_ratio │
                       │ severity_level│
                       │ remark       │
                       │ status       │
                       │ created_at   │
                       │ updated_at   │
                       └──────┬───────┘
                              │ 1:N
                       ┌──────┴───────┐
                       │    image     │
                       ├──────────────┤
                       │ id (PK)      │
                       │ record_id(FK)│
                       │ image_path   │
                       │ thumb_path   │
                       │ file_size    │
                       │ sort_order   │
                       │ created_at   │
                       └──────────────┘
```

### 6.2 图片存储路径

```
{app_external_files}/
├── images/
│   └── {yyyy-MM-dd}/
│       └── {uuid}.jpg
├── thumb/
│   └── {yyyy-MM-dd}/
│       └── {uuid}_thumb.jpg
└── models/
    ├── default/
    │   ├── default_rice_leaf.onnx
    │   └── model_info.json
    └── {user_model_name}/
        ├── model.onnx
        └── model_info.json
```

缩略图：原图等比例缩放到 300×400。

---

## 7. 数据模块

### 7.1 记录列表

与 V1 保持一致：
- 分页加载（每页 20 条）
- 每条显示：缩略图、病害名称、叶片位、级别、日期
- 支持下拉刷新 / 上拉加载更多
- 添加批量删除功能

### 7.2 记录详情

- 显示字段：原图、样点、株号、叶片位、病害名称、病斑面积占比、病情级别、症状描述、记录时间
- 备注可编辑
- 支持 [修改] / [删除]（确认对话框）

### 7.3 数据导出

- 在 App 端使用 Apache POI 直接生成 Excel
- 表头：样点 | 株号 | 叶片位 | 病害名称 | 病斑面积占比(%) | 病情级别(0-9) | 备注
- 通过 Android SAF（Storage Access Framework）或 DownloadManager 保存
- 文件名：`rice-leaf-records-{yyyyMMdd}.xlsx`

---

## 8. 设置模块

### 8.1 功能项

| 分组 | 设置项 | 类型 | 说明 |
|------|--------|------|------|
| 模型管理 | 当前模型 | 下拉 | 显示已安装模型列表，选择切换 |
| 模型管理 | 添加模型 | 按钮 | 从文件/URL 导入 |
| 模型管理 | 检查更新 | 按钮 | 检查默认模型是否有新版本 |
| 拍照参数 | 分辨率 | 下拉 | 低(720p) / 中(1080p) / 高(4K) |
| 拍照参数 | 闪光灯 | 下拉 | 关 / 开 / 自动 |
| 拍照参数 | 保存原图到本地 | 开关 | true / false |
| 拍照参数 | 结果弹窗显示时长 | 滑块 | 1~10 秒，默认 3 秒 |
| 数据管理 | 导出格式 | 下拉 | Excel / CSV |
| 数据管理 | 清空本地数据 | 按钮 | 确认后删除所有记录和图片 |
| 关于 | 版本号 | 只读 | v2.0.0 |
| 关于 | 使用说明 | 页面 | 操作指引 |
| 关于 | 病害对照表 | 页面 | 6 种病害的症状和病斑特征 |
| 关于 | 模型信息 | 页面 | 当前模型详情（类别、输入尺寸、版本） |

---

## 9. 项目结构

```
rice-leaf-local/
├── build.gradle.kts
├── settings.gradle.kts
├── gradle.properties
├── app/
│   ├── build.gradle.kts
│   └── src/
│       └── main/
│           ├── assets/
│           │   └── models/
│           │       └── default/
│           │           ├── default_rice_leaf.onnx
│           │           └── model_info.json
│           ├── java/com/riceleaf/local/
│           │   ├── RiceLeafApp.kt
│           │   ├── MainActivity.kt
│           │   ├── AppSettings.kt
│           │   ├── di/
│           │   │   └── AppModule.kt
│           │   ├── data/
│           │   │   ├── local/
│           │   │   │   ├── AppDatabase.kt
│           │   │   │   ├── dao/
│           │   │   │   └── entity/
│           │   │   └── repository/
│           │   │       └── RecordRepository.kt
│           │   ├── domain/
│           │   │   └── model/
│           │   │       └── DetectResult.kt
│           │   ├── ml/
│           │   │   ├── ModelManager.kt
│           │   │   ├── OnnxInferenceEngine.kt
│           │   │   ├── ImagePreprocessor.kt
│           │   │   ├── LesionEstimator.kt
│           │   │   └── SeverityMapper.kt
│           │   ├── ui/
│           │   │   ├── camera/
│           │   │   │   ├── CameraScreen.kt
│           │   │   │   └── CameraViewModel.kt
│           │   │   ├── data/
│           │   │   │   ├── DataListScreen.kt
│           │   │   │   ├── DataDetailScreen.kt
│           │   │   │   └── DataViewModel.kt
│           │   │   ├── settings/
│           │   │   │   ├── SettingsScreen.kt
│           │   │   │   └── SettingsViewModel.kt
│           │   │   ├── navigation/
│           │   │   │   └── AppNavigation.kt
│           │   │   └── theme/
│           │   │       └── Theme.kt
│           │   └── util/
│           │       ├── ImageCompressor.kt
│           │       ├── FileUtil.kt
│           │       └── ExcelExportUtil.kt
│           └── res/
│               ├── values/
│               │   ├── strings.xml
│               │   └── themes.xml
│               └── drawable/
```

---

## 10. 开发阶段规划

| 阶段 | 内容 | 产出 |
|------|------|------|
| **Phase 1** | 项目骨架：Hilt 注入、Room 建表、导航框架、主题 | 可运行的空 App |
| **Phase 2** | 模型管理模块：默认模型加载、ONNX 推理引擎、图片预处理 | Assets 模型加载并跑通推理 |
| **Phase 3** | 拍照模块：CameraX + 三段按钮 UI + 参数递推 + 结果弹窗 | 完整拍照→识别→展示流程 |
| **Phase 4** | 数据模块：列表/详情/修改/删除 + Excel 导出 | 完整记录管理 |
| **Phase 5** | 设置模块：模型切换/导入/更新 + 拍照参数 + 病害对照表 | 完整功能闭环 |
| **Phase 6** | 测试与优化：单元测试、推理性能优化、异常处理 | 生产可用 |

---

## 11. 关键依赖

```kotlin
// ONNX Runtime
implementation("com.microsoft.onnxruntime:onnxruntime-android:1.18.0")

// CameraX
implementation("androidx.camera:camera-core:1.3.1")
implementation("androidx.camera:camera-camera2:1.3.1")
implementation("androidx.camera:camera-lifecycle:1.3.1")
implementation("androidx.camera:camera-view:1.3.1")

// Room
implementation("androidx.room:room-runtime:2.6.1")
implementation("androidx.room:room-ktx:2.6.1")
ksp("androidx.room:room-compiler:2.6.1")

// Hilt
implementation("com.google.dagger:hilt-android:2.50")
ksp("com.google.dagger:hilt-android-compiler:2.50")

// Excel Export
implementation("org.apache.poi:poi-ooxml:5.2.5")

// Compose + Navigation + Coil（与 V1 相同）
```

---

## 12. 补充功能建议

1. **批量模式**：长按拍摄按钮进入连拍模式，一次拍多张叶片，统一分析
2. **历史对比**：同一株号不同时间点的检测记录对比，展示病害发展趋势
3. **语音播报**：识别结果通过 TTS 语音播报，方便烈日下不看屏幕操作
4. **位置标记**：拍照时记录 GPS 坐标，在地图上查看病害分布
5. **自动备份**：WiFi 环境下自动备份记录到用户配置的服务器/网盘
6. **拍照引导**：取景框叠加叶片轮廓引导线，帮助用户正确摆放叶片
7. **暗光模式**：检测到低光照时自动开启闪光灯 / 提高曝光
8. **统计图表**：按时间/样点/病害维度生成柱状图和趋势线

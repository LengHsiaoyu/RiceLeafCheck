# 水稻叶片病害检查系统 — 项目说明

## 项目背景

用于水稻叶片病害检查，规范水稻叶片病害的检查流程，确保病害识别准确、数据记录统一，为病害预警和防控提供可靠依据。

## 技术栈

- **后端**：Java 17 + Spring Boot 3.x + Spring Data JPA
- **数据库**：MySQL 8.0（生产）/ H2（开发阶段）
- **文件存储**：服务器本地磁盘，按日期分目录
- **Android 端**：Kotlin / Jetpack Compose + CameraX + Retrofit + OkHttp
- **深度学习模型**（后续迭代）：Python 微服务（FastAPI + PyTorch），初期阶段后端返回模拟识别结果
- **构建工具**：Gradle（Android）、Maven（后端）

## 系统架构

```
Android 客户端 (CameraX + Retrofit)
        │  HTTPS / JSON
        ↓
Spring Boot 后端 (Controller → Service → Repository)
        │
   ┌────┴─────────────┐
   ↓                  ↓
MySQL / H2      文件存储 (叶片图片 + 缩略图)
```

- 识别模块使用**策略模式预留接口**，初期返回模拟结果，后续对接真实 Python 推理微服务
- Android 通过 Retrofit + OkHttp 与后端通信，上传 multipart/form-data 图片

## 后端 API 设计

基础路径: `/api/v1`

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/diseases/detect` | 上传叶片图片，返回识别结果 |
| GET | `/records?page=1&size=20` | 分页查询历史记录列表 |
| GET | `/records/{id}` | 查询单条记录详情 |
| PUT | `/records/{id}` | 修改记录（更正识别结果等） |
| DELETE | `/records/{id}` | 逻辑删除某条记录 |
| GET | `/records/export?format=xlsx` | 导出记录为 Excel/CSV |
| GET | `/diseases/list` | 获取系统支持的病害列表 |

### 核心接口详情

**病害检测 `POST /diseases/detect`**

```
Request:  multipart/form-data
  - image:          图片文件 (JPEG/PNG, ≤10MB)
  - samplePoint:    样点编号
  - plantNo:        株号
  - leafPosition:   叶片位 (倒1叶/倒2叶/倒3叶)

Response: {
  "recordId": 1,
  "diseaseName": "稻瘟病（叶瘟）",
  "confidence": 0.92,
  "lesionAreaRatio": 15.5,
  "severityLevel": 5,
  "symptomDesc": "梭形斑，边缘褐色，中央灰白色",
  "imageUrl": "/images/2024-05-18/xxx.jpg",
  "createTime": "2024-05-18T10:30:00"
}
```

初期阶段 diseaseName / confidence / lesionAreaRatio / severityLevel 返回模拟数据。

**记录列表 `GET /records?page=1&size=20`**

```
Response: {
  "total": 100,
  "page": 1,
  "records": [{
    "id": 1,
    "thumbnailUrl": "/images/thumb/xxx.jpg",
    "diseaseName": "稻瘟病（叶瘟）",
    "severityLevel": 5,
    "createTime": "2024-05-18T10:30:00"
  }]
}
```

**数据导出 `GET /records/export?format=xlsx`**

后端使用 Apache POI 生成 Excel，返回文件流，包含表头：样点、株号、叶片位、病害名称、病斑面积占比、病情级别、备注。

## Android 客户端 UI 设计

### 底部导航

三 Tab 通过 BottomNavigationView 切换：拍照、数据、设置。进入拍照 Tab 时自动开启 CameraX 相机预览。

### 拍照界面

```
┌──────────────────────────────────────────────┐
│               CameraX 实时预览                 │
│            (取景框 + 叶片轮廓引导线)            │
├──────────────────────────────────────────────┤
│  样点: [   ]  株号: [   ]  叶片位: [倒1▼]     │
├──────────────────────────────────────────────┤
│  [已拍: 2 张]          [⭕ 拍摄]  [继续拍摄 →] │
└──────────────────────────────────────────────┘
```

交互流程：

1. 进入页面自动开启相机预览
2. 填写样点、株号、叶片位（下拉选择：倒1叶/倒2叶/倒3叶）
3. 点击**拍摄** → 自动上传后端 → 返回识别结果 → 半屏弹出结果卡片
4. 点击**继续拍摄** → 不退出预览，参数保留，可连续拍摄
5. 继续拍摄时参数自动递推：
   - 叶片位自动递推（倒3叶 → 倒2叶 → 倒1叶 → 倒3叶...循环）
   - 叶片位递推到下一轮时株号自动 +1
   - 所有参数均可手动修改覆盖
6. 角标 `[已拍: N 张]` 显示当前批次拍摄数量

**结果卡片（半屏弹出）**：

```
┌─────────────────────────────────────┐
│  ✓ 识别完成                         │
│  病害: 稻瘟病（叶瘟）  置信度: 92%   │
│  病斑面积: 15.5%       病情级别: 5   │
│  [重拍]              [确认 & 继续]   │
└─────────────────────────────────────┘
```

### 数据列表界面

```
┌────────────────────────────────────────────────┐
│  历史记录                           [导出 ▼]    │
├────────────────────────────────────────────────┤
│  ┌─────────┬──────────────────────────────┐    │
│  │ 缩略图   │ 稻瘟病（叶瘟）                │    │
│  │ [叶片]   │ 倒3叶 | 级别 5 | 05-18       │    │
│  └─────────┴──────────────────────────────┘    │
│  ┌─────────┬──────────────────────────────┐    │
│  │ 缩略图   │ 白叶枯病                      │    │
│  │ [叶片]   │ 倒1叶 | 级别 3 | 05-17       │    │
│  └─────────┴──────────────────────────────┘    │
│              (下拉加载更多...)                   │
└────────────────────────────────────────────────┘
```

点击某行进入**详情页**：显示原图大图、完整字段信息，备注可编辑，提供修改/删除操作。

### 设置界面

```
┌────────────────────────────────────┐
│  拍照参数                          │
│    分辨率        [ 高(1080p) ▼ ]   │
│    闪光灯        [ 自动 ▼ ]        │
│    保存原图到本地  [ ✓ 开关 ]       │
├────────────────────────────────────┤
│  数据管理                          │
│    导出格式       [ Excel ▼ ]      │
│    清空本地缓存    [ 清空 ]         │
├────────────────────────────────────┤
│  关于                              │
│    版本号         v1.0.0           │
│    使用说明       点击查看 →        │
│    病害对照表     点击查看 →        │
└────────────────────────────────────┘
```

服务器配置项不在前端展示，通过编译期或配置文件内置。

## 数据库设计

### 病害字典表 `disease_dict`

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint | 主键，自增 |
| name | varchar(64) | 病害名称 |
| symptom | varchar(256) | 典型症状描述 |
| spot_feature | varchar(256) | 病斑特征 |
| sort_order | int | 排序 |

### 检测记录表 `record`

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint | 主键，自增 |
| sample_point | varchar(32) | 样点编号 |
| plant_no | varchar(32) | 株号 |
| leaf_position | varchar(16) | 叶片位（倒1叶/倒2叶/倒3叶） |
| disease_id | bigint | 关联病害字典 |
| confidence | decimal(5,4) | 置信度（0.0000~1.0000） |
| lesion_ratio | decimal(5,2) | 病斑面积占比（%） |
| severity_level | tinyint | 病情级别（0/1/3/5/7/9） |
| remark | varchar(256) | 备注 |
| status | tinyint | 0=正常, 1=已删除（软删除） |
| created_at | datetime | 创建时间 |
| updated_at | datetime | 修改时间 |

### 检测图片表 `image`

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint | 主键，自增 |
| record_id | bigint | 关联检测记录 |
| image_path | varchar(256) | 原图路径 |
| thumb_path | varchar(256) | 缩略图路径 |
| file_size | bigint | 文件大小（bytes） |
| sort_order | int | 同记录多张图片排序 |
| created_at | datetime | 创建时间 |

## 核心数据流

### 拍照识别流程

```
Android → POST /detect (图片 + 样点/株号/叶片位)
         → 后端保存图片到磁盘
         → 调用识别接口（初期返回模拟结果）
         → 写入 record 表 & image 表
         → 生成缩略图
         → 返回完整记录到客户端
         → 客户端展示结果卡片
```

### 连续拍摄递推规则

| 操作 | 叶片位变化 | 株号变化 |
|------|-----------|---------|
| 继续拍摄 | 倒3→倒2→倒1→倒3... | 循环一轮后株号 +1 |

所有字段均可手动输入覆盖自动递推值。

## 常见水稻叶片病害快速识别表

| 病害名称 | 典型症状 | 病斑特征 |
|---------|---------|---------|
| 稻瘟病（叶瘟） | 梭形或圆形斑，边缘褐色，中央灰白色 | 潮湿时背面有灰色霉层 |
| 纹枯病 | 叶鞘及叶片上云纹状大斑，边缘深褐色 | 病部可见白色菌丝团或褐色菌核 |
| 白叶枯病 | 沿叶缘或叶脉呈黄白色长条状，病部与健部界限呈波浪纹 | 湿度大时表面有淡黄色菌脓 |
| 细菌性条斑病 | 细条状半透明水渍斑，对光看呈半透明 | 干燥后呈白色，有无数小条斑 |
| 胡麻叶斑病 | 芝麻粒大小褐色至红褐色斑点，边缘黄色晕圈 | 严重时连成不规则大斑 |

## 数据记录格式

在 App 端以列表形式展示，支持导出 Excel。

| 样点 | 株号 | 叶片位（倒1/倒2/倒3） | 病害名称 | 病斑面积占比（%） | 病情级别（0-9） | 备注 |
|------|------|----------------------|---------|-----------------|---------------|------|
| 图片1 | 1 | 倒3叶 | | | | |
| 图片2 | 2 | 倒2叶 | | | | |

平均病情指数：病情级别参照国标（0=无病，1=≤5%，3=6~10%，5=11~25%，7=26~50%，9=≥51%）。

# 水稻叶片病害检查系统 — 软件功能设计书（SDD）

## 1. 引言

### 1.1 目的

本文档定义水稻叶片病害检查系统的完整功能与软件设计，作为后续开发、测试和验收的依据。

### 1.2 范围

系统覆盖 Android 移动端和 Java 后端服务，实现叶片图像采集、病害识别、数据记录管理、报表导出全流程。

### 1.3 定义与术语

| 术语 | 说明 |
|------|------|
| SDD | Software Design Document，软件设计文档 |
| 叶片位 | 水稻植株上叶片的生长位置，从顶部向下数（倒1叶为最上端叶片） |
| 病情级别 | 按国标分为 0/1/3/5/7/9 六级，以病斑面积占比划分 |

### 1.4 参考资料

- 水稻病害测报调查规范（GB/T 15794）
- 常见水稻叶片病害快速识别表（项目内部）

---

## 2. 总体概述

### 2.1 系统目标

规范水稻叶片病害田间检查流程，通过移动端拍摄 + 后端 AI 识别，实现病害的标准化识别与数据统一管理，为病害预警和精准防控提供数据支撑。

### 2.2 用户角色

| 角色 | 职责 |
|------|------|
| 田间调查员 | 使用 Android App 拍摄叶片、查看记录、导出数据 |
| 系统管理员 | 管理病害字典、维护服务器、监控系统状态 |

### 2.3 运行环境

| 层次 | 技术选型 |
|------|---------|
| Android 端 | Android 8.0+ (API 26+) |
| 后端 | Java 17, Spring Boot 3.x, Maven |
| 数据库 | MySQL 8.0 (生产), H2 (开发) |
| 文件存储 | 服务器本地磁盘 |
| 深度学习 | Python 微服务 (后续迭代) |

---

## 3. 系统架构

### 3.1 整体架构

```
┌─────────────────────────────────────────────────┐
│                  Android 客户端                   │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐       │
│  │  拍照模块  │  │  数据模块  │  │  设置模块  │       │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘       │
│       └──────────────┼──────────────┘             │
│               ┌──────┴──────┐                    │
│               │  网络层       │                    │
│               │ Retrofit+OkHttp│                  │
│               └──────┬──────┘                    │
└──────────────────────┼───────────────────────────┘
                       │ HTTPS / JSON
┌──────────────────────┼───────────────────────────┐
│                Java 后端 (Spring Boot)            │
│  ┌───────────────────┴──────────────────────┐    │
│  │              REST API (Controller)         │    │
│  ├──────────────────────────────────────────┤    │
│  │   DiseaseService   │   RecordService      │    │
│  │   (识别策略接口)    │   (CRUD + Excel导出)  │    │
│  ├────────────────────┴──────────────────────┤    │
│  │           Repository (JPA)                │    │
│  ├───────────────────────────────────────────┤    │
│  │   MySQL/H2            文件系统             │    │
│  └───────────────────────────────────────────┘    │
└──────────────────────────────────────────────────┘
```

### 3.2 识别策略模式设计

初期阶段 ML 模型未就绪，采用策略模式预留扩展点：

```
DiseaseDetectStrategy (接口)
    ├── MockDiseaseDetectStrategy    (当前：返回模拟数据)
    └── ModelDiseaseDetectStrategy   (后续：对接 Python ML 微服务)
```

- 通过 Spring `@ConditionalOnProperty` 或配置文件切换实现
- 模拟策略随机返回 5 种病害之一 + 合理范围内的置信度与病斑面积

---

## 4. 模块功能设计

### 4.1 拍照模块

**4.1.1 功能描述**

田间调查员打开 App 后通过拍照 Tab 进入相机预览界面，拍摄水稻叶片，系统自动上传图片并由后端识别病害，返回结果在界面上展示。

**4.1.2 交互流程**

```这里我希望拍照完成后再填写参数
  [进入拍照界面] → CameraX自动开启预览
       ↓
  [填写参数] → 样点编号、株号、叶片位
       ↓
  [点击拍摄] → 拍照 → 压缩 → 上传后端
       ↓
  [展示结果卡片] → 病害名称、置信度、病斑面积、病情级别
       ↓
  [用户操作] → 确认 / 重拍
       ↓
  [继续拍摄] → 参数递推 → 回到预览 (循环)
```

**4.1.3 连续拍摄参数递推规则**

```
叶片位: 倒3叶 → 倒2叶 → 倒1叶 → (株号+1, 回到倒3叶) → ...
```

递推逻辑在前端执行：

```kotlin
// 伪代码
fun onContinueShoot() {
    leafPosition = when (leafPosition) {
        "倒3叶" -> "倒2叶"
        "倒2叶" -> "倒1叶"
        "倒1叶" -> {
            plantNo += 1
            "倒3叶"
        }
        else -> "倒3叶"
    }
    // 所有字段保持可编辑状态，用户可手动覆盖
}
```

**4.1.4 状态与约束**

| 状态 | 条件 | 行为 |
|------|------|------|
| 相机未授权 | 首次进入/权限被拒 | 弹窗引导授权 |
| 上传中 | 点击拍摄后 | 显示加载动画，按钮不可点击 |
| 上传失败 | 网络异常/超时 | 提示重试，保留已填参数 |
| 识别完成 | 服务器返回结果 | 半屏弹出结果卡片 |

### 4.2 数据模块

**4.2.1 功能描述**

查看历史检测记录，支持列表浏览、详情查看、修改/删除，以及数据导出。

**4.2.2 列表页**

- 分页加载（每页 20 条）
- 每条显示：缩略图、病害名称、叶片位、级别、日期
- 支持下拉刷新 / 上拉加载更多
- 顶部 [导出] 按钮 → 选择格式（Excel/CSV）→ 后端生成文件 → 系统下载管理器保存

**4.2.3 详情页**

- 显示字段：原图、样点、株号、叶片位、病害名称、病斑面积占比、病情级别、症状描述、记录时间
- 备注字段可编辑
- 提供 [修改] 按钮保存编辑，[删除] 按钮逻辑删除（确认对话框）

**4.2.4 数据导出**

- 请求：`GET /records/export?format=xlsx&startDate=&endDate=`
- 后端使用 Apache POI 生成 .xlsx 文件
- 表头：样点 | 株号 | 叶片位 | 病害名称 | 病斑面积占比(%) | 病情级别(0-9) | 备注
- 返回 Content-Type：`application/vnd.openxmlformats-officedocument.spreadsheetml.sheet`

### 4.3 设置模块

**4.3.1 功能描述**

提供拍照参数调整、数据管理、系统信息查看。

**4.3.2 功能项**

| 分组 | 设置项 | 类型 | 可选值 | 默认值 |
|------|--------|------|--------|--------|
| 拍照参数 | 分辨率 | 下拉 | 低(720p) / 中(1080p) / 高(4K) | 中(1080p) |
| 拍照参数 | 闪光灯 | 下拉 | 关 / 开 / 自动 | 自动 |
| 拍照参数 | 保存原图到本地 | 开关 | true / false | true |
| 数据管理 | 导出格式 | 下拉 | Excel (.xlsx) / CSV | Excel |
| 数据管理 | 清空本地缓存 | 按钮 | — | — |
| 关于 | 版本号 | 只读文本 | — | v1.0.0 |
| 关于 | 使用说明 | 页面跳转 | — | — |
| 关于 | 病害对照表 | 页面跳转 | — | — |

**注意：服务器配置不在前端暴露，通过编译期常量或 `local.properties` 内置。**

- `local.properties` 中配置 `SERVER_BASE_URL=http://<ip>:<port>/api/v1`
- OkHttp 拦截器中自动读取，不提供 UI 暴露

### 4.4 病害对照表模块

**4.4.1 功能描述**

内嵌在"设置 → 病害对照表"中，方便田间调查员在拍摄现场快速比对症状。

**4.4.2 数据内容**

| 病害名称 | 典型症状 | 病斑特征 |
|---------|---------|---------|
| 稻瘟病（叶瘟） | 梭形或圆形斑，边缘褐色，中央灰白色 | 潮湿时背面有灰色霉层 |
| 纹枯病 | 叶鞘及叶片上云纹状大斑，边缘深褐色 | 病部可见白色菌丝团或褐色菌核 |
| 白叶枯病 | 沿叶缘或叶脉呈黄白色长条状，病部与健部界限呈波浪纹 | 湿度大时表面有淡黄色菌脓 |
| 细菌性条斑病 | 细条状半透明水渍斑，对光看呈半透明 | 干燥后呈白色，有无数小条斑 |
| 胡麻叶斑病 | 芝麻粒大小褐色至红褐色斑点，边缘黄色晕圈 | 严重时连成不规则大斑 |

后续可扩展更多病害（稻曲病、恶苗病、条纹叶枯病等）。

---

## 5. 接口定义

### 5.1 接口规范

| 项目 | 规范 |
|------|------|
| 基础路径 | `/api/v1` |
| 数据格式 | JSON (application/json) |
| 图片上传 | multipart/form-data |
| 认证方案 | 初期无认证（后续迭代加入 Token 认证） |
| 错误格式 | `{"code": 400, "message": "错误描述", "timestamp": "..."}` |

### 5.2 接口清单

#### 5.2.1 病害检测

- **路径**：`POST /api/v1/diseases/detect`
- **请求**：multipart/form-data

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| image | File | 是 | JPEG/PNG，≤10MB |
| samplePoint | String | 是 | 样点编号 |
| plantNo | String | 是 | 株号 |
| leafPosition | String | 是 | 倒1叶 / 倒2叶 / 倒3叶 |

- **响应**：

```json
{
  "code": 200,
  "data": {
    "recordId": 1,
    "diseaseName": "稻瘟病（叶瘟）",
    "confidence": 0.92,
    "lesionAreaRatio": 15.5,
    "severityLevel": 5,
    "symptomDesc": "梭形斑，边缘褐色，中央灰白色",
    "imageUrl": "/images/2024-05-18/abc123.jpg",
    "thumbnailUrl": "/images/thumb/2024-05-18/abc123.jpg",
    "createTime": "2024-05-18T10:30:00"
  }
}
```

- **处理逻辑**：
  1. 校验图片格式与大小
  2. 保存原图到 `/{base-dir}/{yyyy-MM-dd}/{uuid}.jpg`
  3. 生成缩略图（300×400，保持宽高比）
  4. 调用识别策略接口（初期返回模拟数据）
  5. 写入 record 表 + image 表
  6. 返回完整记录

#### 5.2.2 查询记录列表

- **路径**：`GET /api/v1/records`
- **参数**：`page`(默认1), `size`(默认20, 最大50), `startDate`, `endDate`, `diseaseId`
- **响应**：

```json
{
  "code": 200,
  "data": {
    "total": 100,
    "page": 1,
    "size": 20,
    "records": [
      {
        "id": 1,
        "samplePoint": "图片1",
        "plantNo": "1",
        "leafPosition": "倒3叶",
        "diseaseName": "稻瘟病（叶瘟）",
        "severityLevel": 5,
        "thumbnailUrl": "/images/thumb/2024-05-18/abc123.jpg",
        "createTime": "2024-05-18T10:30:00"
      }
    ]
  }
}
```

- **排序**：按 createTime 降序

#### 5.2.3 查询记录详情

- **路径**：`GET /api/v1/records/{id}`
- **响应**：返回 record + image 完整信息

#### 5.2.4 修改记录

- **路径**：`PUT /api/v1/records/{id}`
- **请求体**：

```json
{
  "samplePoint": "图片1",
  "plantNo": "1",
  "leafPosition": "倒3叶",
  "diseaseId": 1,
  "lesionAreaRatio": 15.5,
  "severityLevel": 5,
  "remark": "已二次确认"
}
```

- **约束**：仅允许修改非识别结果字段（手动校正场景）

#### 5.2.5 删除记录

- **路径**：`DELETE /api/v1/records/{id}`
- **实现**：逻辑删除，`status` 字段设为 1，图片文件不删除

#### 5.2.6 导出数据

- **路径**：`GET /api/v1/records/export`
- **参数**：`format`(xlsx/csv), `startDate`, `endDate`
- **响应**：二进制文件流
- **文件名**：`rice-leaf-records-{yyyyMMdd}.xlsx`

#### 5.2.7 病害列表

- **路径**：`GET /api/v1/diseases/list`
- **响应**：返回 disease_dict 全表，供 App 本地缓存

---

## 6. 数据库设计

### 6.1 E-R 图

```
┌──────────────┐       ┌──────────────┐
│ disease_dict │       │    record    │
├──────────────┤       ├──────────────┤
│ id (PK)      │ ←──   │ id (PK)      │
│ name         │       │ sample_point │
│ symptom      │       │ plant_no     │
│ spot_feature │       │ leaf_position│
│ sort_order   │       │ disease_id(FK)│
└──────────────┘       │ confidence   │
                       │ lesion_ratio │
                       │ severity_level│
                       │ remark       │
                       │ status       │
                       │ created_at   │
                       │ updated_at   │
                       └──────┬───────┘
                              │ 1:N
                       ┌──────┴───────┐
                       │    image     │
                       ├──────────────┤
                       │ id (PK)      │
                       │ record_id(FK)│
                       │ image_path   │
                       │ thumb_path   │
                       │ file_size    │
                       │ sort_order   │
                       │ created_at   │
                       └──────────────┘
```

### 6.2 DDL

```sql
CREATE TABLE disease_dict (
    id          BIGINT AUTO_INCREMENT PRIMARY KEY,
    name        VARCHAR(64)  NOT NULL COMMENT '病害名称',
    symptom     VARCHAR(256) NOT NULL DEFAULT '' COMMENT '典型症状',
    spot_feature VARCHAR(256) NOT NULL DEFAULT '' COMMENT '病斑特征',
    sort_order  INT          NOT NULL DEFAULT 0 COMMENT '排序',
    UNIQUE KEY uk_name (name)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='病害字典表';

CREATE TABLE record (
    id              BIGINT AUTO_INCREMENT PRIMARY KEY,
    sample_point    VARCHAR(32)  NOT NULL COMMENT '样点编号',
    plant_no        VARCHAR(32)  NOT NULL COMMENT '株号',
    leaf_position   VARCHAR(16)  NOT NULL COMMENT '叶片位',
    disease_id      BIGINT       DEFAULT NULL COMMENT '病害ID',
    confidence      DECIMAL(5,4) DEFAULT NULL COMMENT '置信度',
    lesion_ratio    DECIMAL(5,2) DEFAULT NULL COMMENT '病斑面积占比(%)',
    severity_level  TINYINT      DEFAULT NULL COMMENT '病情级别(0/1/3/5/7/9)',
    remark          VARCHAR(256) DEFAULT '' COMMENT '备注',
    status          TINYINT      NOT NULL DEFAULT 0 COMMENT '0=正常 1=已删除',
    created_at      DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at      DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_created_at (created_at),
    INDEX idx_sample (sample_point, plant_no)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='检测记录表';

CREATE TABLE image (
    id          BIGINT AUTO_INCREMENT PRIMARY KEY,
    record_id   BIGINT       NOT NULL COMMENT '关联记录ID',
    image_path  VARCHAR(256) NOT NULL COMMENT '原图路径',
    thumb_path  VARCHAR(256) DEFAULT NULL COMMENT '缩略图路径',
    file_size   BIGINT       DEFAULT 0 COMMENT '文件大小(bytes)',
    sort_order  INT          DEFAULT 0 COMMENT '排序',
    created_at  DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_record_id (record_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='检测图片表';
```

### 6.3 种子数据

```sql
INSERT INTO disease_dict (name, symptom, spot_feature, sort_order) VALUES
('稻瘟病（叶瘟）', '梭形或圆形斑，边缘褐色，中央灰白色', '潮湿时背面有灰色霉层', 1),
('纹枯病', '叶鞘及叶片上云纹状大斑，边缘深褐色', '病部可见白色菌丝团或褐色菌核', 2),
('白叶枯病', '沿叶缘或叶脉呈黄白色长条状，病部与健部界限呈波浪纹', '湿度大时表面有淡黄色菌脓', 3),
('细菌性条斑病', '细条状半透明水渍斑，对光看呈半透明', '干燥后呈白色，有无数小条斑', 4),
('胡麻叶斑病', '芝麻粒大小褐色至红褐色斑点，边缘黄色晕圈', '严重时连成不规则大斑', 5);
```

---

## 7. 数据流设计

### 7.1 拍照识别全流程

```
┌────────┐         ┌──────────┐        ┌──────────┐       ┌────────┐
│ 用户操作 │         │  Android  │        │ Spring   │       │  MySQL  │
│         │         │  Client   │        │  Boot    │       │         │
└───┬─────┘         └────┬──────┘        └────┬─────┘       └───┬─────┘
    │                     │                    │                  │
    │ 1.填写参数          │                    │                  │
    │───────────────────>│                    │                  │
    │                     │                    │                  │
    │ 2.点击拍摄           │                    │                  │
    │───────────────────>│                    │                  │
    │                     │ 3. CameraX拍照     │                  │
    │                     │ 4. POST /detect    │                  │
    │                     │──────────────────>│                  │
    │                     │                    │ 5. 保存图片文件   │
    │                     │                    │ 6. 生成缩略图     │
    │                     │                    │ 7. 调用识别策略   │
    │                     │                    │ 8. INSERT record │
    │                     │                    │──────────────────>│
    │                     │                    │ 9. INSERT image   │
    │                     │                    │──────────────────>│
    │                     │  10. 返回识别结果   │                  │
    │                     │<──────────────────│                  │
    │                     │                    │                  │
    │ 11. 展示结果卡片     │                    │                  │
    │<───────────────────│                    │                  │
    │                     │                    │                  │
    │ 12. 确认/继续拍摄    │                    │                  │
    │───────────────────>│                    │                  │
    │                     │                    │                  │
```

### 7.2 数据导出流程

```
  Android: 点击 [导出] → 选择格式 (Excel/CSV)
       ↓
  Android: GET /records/export?format=xlsx&startDate=&endDate=
       ↓
  后端: RecordService 查询记录 (status=0, 按日期过滤)
       ↓
  后端: Apache POI 生成 Excel 工作簿
       ↓
  后端: ResponseEntity<byte[]> 返回文件流
       ↓
  Android: 通过 DownloadManager 或 SAF 写入本地
```

### 7.3 图片文件管理

```
存储路径: {base-dir}/{yyyy-MM-dd}/{uuid}.{ext}
缩略图:   {base-dir}/thumb/{yyyy-MM-dd}/{uuid}_thumb.jpg

清理策略: record 逻辑删除时图片保留（支持数据恢复）
          后续可通过定时任务清理已删除记录超过 N 天的图片
```

---

## 8. 非功能性需求

### 8.1 性能要求

| 指标 | 要求 |
|------|------|
| 图片上传 | 单张 ≤ 10MB，上传 + 识别响应用 ≤ 5s（2G 网络下 ≤ 10s） |
| 列表加载 | 首页数据加载 ≤ 2s |
| 并发支持 | 初期支持 10 个并发请求 |
| 图片压缩 | 上传前压缩至 1920px 宽边，JPEG 质量 85% |

### 8.2 可用性

- 网络异常时，拍照界面不退出（保留预览和已填参数）
- 上传失败支持重试（自动重试 1 次，失败后提示用户手动重试）
- App 无网络时可查看已缓存的历史记录（本地 Room 数据库）

### 8.3 安全性

- 上传图片校验：仅允许 JPEG/PNG，MIME type + 文件头双重校验
- 文件大小限制：后端限制 10MB
- SQL 注入防护：使用 JPA 参数化查询
- API 限流：后续迭代加入（初期不做）

### 8.4 可维护性

- 识别模块通过策略接口隔离，替换实现不影响业务层
- 配置项集中管理（Spring `application.yml` + Android `local.properties`）
- 日志规范：使用 SLF4J，异常全量记录堆栈

### 8.5 兼容性

- Android 最低支持 API 26 (Android 8.0)
- 适配主流屏幕分辨率（720p ~ 1440p）
- CameraX 向下兼容到 Camera2 API 级别 LEGACY

---

## 9. 项目结构

### 9.1 后端模块

```
rice-leaf-server/
├── pom.xml
├── src/main/java/com/riceleaf/
│   ├── RiceLeafApplication.java
│   ├── config/
│   │   ├── WebConfig.java              // CORS 配置
│   │   └── StrategyConfig.java          // 识别策略 Bean 注入
│   ├── controller/
│   │   ├── DiseaseController.java       // /diseases/**
│   │   └── RecordController.java        // /records/**
│   ├── service/
│   │   ├── DiseaseService.java
│   │   ├── RecordService.java
│   │   └── strategy/
│   │       ├── DiseaseDetectStrategy.java        // 策略接口
│   │       └── MockDiseaseDetectStrategy.java    // 模拟实现
│   ├── repository/
│   │   ├── DiseaseDictRepository.java
│   │   ├── RecordRepository.java
│   │   └── ImageRepository.java
│   ├── entity/
│   │   ├── DiseaseDict.java
│   │   ├── Record.java
│   │   └── Image.java
│   ├── dto/
│   │   ├── DetectRequest.java
│   │   ├── DetectResponse.java
│   │   ├── RecordListResponse.java
│   │   └── ApiResponse.java
│   ├── util/
│   │   ├── ImageUtil.java              // 图片压缩、缩略图生成
│   │   └── ExcelUtil.java              // Apache POI 导出
│   └── exception/
│       ├── GlobalExceptionHandler.java
│       └── BusinessException.java
└── src/main/resources/
    ├── application.yml
    ├── application-dev.yml
    └── application-prod.yml
```

### 9.2 Android 模块

```
rice-leaf-app/
├── build.gradle.kts
├── src/main/java/com/riceleaf/app/
│   ├── RiceLeafApp.kt                  // Application
│   ├── MainActivity.kt                 // 底部导航容器
│   ├── di/
│   │   └── AppModule.kt                // Hilt 依赖注入
│   ├── data/
│   │   ├── remote/
│   │   │   ├── ApiService.kt           // Retrofit 接口定义
│   │   │   ├── RetrofitClient.kt       // Retrofit 单例
│   │   │   └── dto/                    // 网络层 DTO
│   │   ├── local/
│   │   │   ├── AppDatabase.kt          // Room 数据库
│   │   │   ├── dao/                    // Room DAO
│   │   │   └── entity/                 // Room Entity
│   │   └── repository/
│   │       └── RecordRepository.kt
│   ├── domain/
│   │   └── model/                      // 领域模型
│   ├── ui/
│   │   ├── camera/
│   │   │   ├── CameraFragment.kt
│   │   │   └── CameraViewModel.kt
│   │   ├── data/
│   │   │   ├── DataListFragment.kt
│   │   │   ├── DataDetailFragment.kt
│   │   │   └── DataViewModel.kt
│   │   └── settings/
│   │       ├── SettingsFragment.kt
│   │       └── SettingsViewModel.kt
│   └── util/
│       ├── ImageCompressor.kt          // 图片压缩
│       └── FileUtil.kt
└── res/
    ├── values/
    │   ├── strings.xml
    │   └── styles.xml
    └── navigation/
        └── nav_graph.xml
```

---

## 10. 开发阶段规划

| 阶段 | 内容 | 产出物 |
|------|------|--------|
| **Phase 1** | 后端基础 CRUD + 模拟识别 | 可运行的后端服务，REST API 全通 |
| **Phase 2** | Android 拍照 + 上传 + 数据模块 | 可拍照上传查看记录、导出 Excel 的完整 App |
| **Phase 3** | 连续拍摄 + 参数递推 + 设置模块 | 完整功能闭环 |
| **Phase 4** | 真实 ML 模型集成 | 替换 Mock 策略为 Python 推理微服务 |
| **Phase 5** | 离线缓存 + 异常处理完善 | Room 本地缓存，网络异常降级 |

---

## 附录 A：病情级别国标参考

| 级别 | 病斑面积占比 | 说明 |
|------|-------------|------|
| 0 | 0% | 无病 |
| 1 | ≤ 5% | 轻度 |
| 3 | 6% ~ 10% | 中轻度 |
| 5 | 11% ~ 25% | 中度 |
| 7 | 26% ~ 50% | 中重度 |
| 9 | ≥ 51% | 重度 |

平均病情指数计算公式：`DI = Σ(各级病叶数 × 各级代表值) / (调查总叶数 × 最高级代表值) × 100`

## 附录 B：错误码表

| 错误码 | 说明 |
|--------|------|
| 400 | 请求参数校验失败 |
| 413 | 图片文件超过大小限制 |
| 415 | 不支持的图片格式 |
| 404 | 记录不存在 |
| 500 | 服务器内部错误 |
